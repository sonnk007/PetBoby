spring:
  config:
    activate:
      on-profile: dev

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.sonnk.product: DEBUG

# Demo Spring Boot fundamentals: cấu hình riêng cho môi trường dev.
petboby:
  product:
    default-page-size: 10
    max-toppings-per-drink: 5

